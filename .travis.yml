language: python
matrix:
  include:
    - python: 3.5
    - python: 3.6
    - python: 3.7
      dist: xenial
      sudo: true 
cache: pip
# install dependencies
install:
  - pip install -r requirements.txt
  - pip install mindsdb
# run tests
script:
  - pytest