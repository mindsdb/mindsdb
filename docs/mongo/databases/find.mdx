---
title: show databases
sidebarTitle: show databases
---

## Description

The `show databases` command lists all data sources connected to MindsDB.

## Syntax

First, switch to `mindsdb` using the `use` command.

```sql
use mindsdb
```

Then, list all data sources connected to MindsDB:

```sql
show databases
```
