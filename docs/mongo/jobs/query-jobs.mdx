---
title: Query Jobs
sidebarTitle: Query Jobs
---

You can query the `jobs` and `jobs_history` tables to find out more about all created jobs and their execution history.

```sql
> db.jobs.find()
> db.jobs_history.find()
```
