---
title: status
sidebarTitle: status
---

## Description

The `status` property of the model stores its status, such as `generating`, `training`, or `complete`.

## Syntax

Here is the syntax:

```sql
model_name.status
```
