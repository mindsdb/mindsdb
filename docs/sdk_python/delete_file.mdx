---
title: Delete Files
sidebarTitle: Delete Files
---

## Description

In MindsDB, files are treated as tables. These are stored in the default `files` database. To delete a file, you must save this `files` database into a variable and then, run the `delete_table()` function on it.

## Syntax

<Info>
This feature is in progress.
</Info>
