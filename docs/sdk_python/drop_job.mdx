---
title: drop_job()
sidebarTitle: drop_job()
---

## Description

The `drop_job()` function deletes a job from MindsDB.

## Syntax

Use the `drop_job()` method to remove a job:

```bash
project.drop_job('job_name')
```
