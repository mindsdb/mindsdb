---
title: drop_project()
sidebarTitle: drop_project()
---

## Description

The `drop_project()` function removed a project from MindsDB.

## Syntax

Use the `drop_project()` method to remove a project:

```bash
server.drop_project('project_name')
```
