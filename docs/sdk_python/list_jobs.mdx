---
title: todo
sidebarTitle: list_jobs
---

## Description

The `list_jobs()` function is executed on a project and lists all jobs available in this project.

## Syntax

Use the `list_jobs()` method to list all jobs in a project:

```bash
project.list_jobs()
```
