---
title: list_models()
sidebarTitle: list_models()
---

## Description

The `list_models()` function lists all models available in a defined project.

## Syntax

Use the `list_models()` method to list all models contained in the project:

```bash
project.list_models()
```
