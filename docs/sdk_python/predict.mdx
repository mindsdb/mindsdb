---
title: predict()
sidebarTitle: predict()
---

## Description

The `predict()` function fetches predictions from the model table.

## Syntax

### Single Prediction

<Info>
This feature is in progress.
</Info>

### Batch Predictions

Use the `predict()` method to make batch predictions be passing the data table as its argument:

```bash
my_model.predict(my_table.limit(10))
```
