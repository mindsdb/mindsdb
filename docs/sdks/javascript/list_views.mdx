---
title: List Views
sidebarTitle: List Views
---

## Description

The getAllViews() function lists all available views.

​
## Syntax

Here is how to list all available views:

```sql
const allViews = await MindsDB.Views.getAllViews();
console.log('all views:')
allViews.forEach(v => {
  console.log(v.name);
});
```
