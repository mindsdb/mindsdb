###############################################
# Academic Research Copilot - .env.example
###############################################

# Required: Google Gemini API key for embeddings
# Get a free key: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# MindsDB connection (used by scripts and SDK)
# For Docker Compose, the service name is "mindsdb"; for local, use "localhost"
MINDSDB_HOST=localhost
MINDSDB_PORT=47334

# Optional: Path for DuckDB when running locally (fallback text search)
# In Docker, the container path is /app/data/academic_papers.duckdb
DUCKDB_PATH=data/academic_papers.duckdb

