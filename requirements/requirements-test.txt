docker >= 5.0.3
hierarchicalforecast
netifaces >= 0.11.0
openai==1.24.0
pytest >= 7.3.0, < 8.0.0
pytest-lazy-fixture~=0.6.3
pytest-subtests
lightwood==24.5.2.0  # This is required for tests/unit/test_executor.py. These tests need to be refactored.
responses
pytest-cov 
coveralls
locust
ollama >= 0.1.7 # Langchain tests
anthropic >= 0.21.3 # Langchain tests