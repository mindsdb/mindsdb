{
    "name": "Jira Read-Only Test",
    "test_type": "read_only",
    "queries": {
        "select_projects": {
            "query": "SELECT * FROM {db_name}.projects LIMIT 5;",
            "expected_columns": ["id", "name", "key"],
            "min_rows": 1
        }
    },
    "negative_tests": [
        {
            "query": "SELECT * FROM {db_name}.non_existent_table;",
            "expected_error": "Table not found: non_existent_table"
        }
    ]
}