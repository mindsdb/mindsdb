{
    "name": "Mariadb Read-Only Test",
    "test_type": "read_only",
    "queries": {
        "select_users": {
            "query": "SELECT * FROM {db_name}.users;",
            "expected_columns": ["id", "username", "email"],
            "min_rows": 1
        },
        "select_by_id": {
            "query": "SELECT * FROM {db_name}.users where id = 2;",
            "expected_columns": ["id", "username", "email"],
            "exact_rows": 1
        }
    },
    "negative_tests": [
        {
            "query": "SELECT * FROM {db_name}.non_existent_table;",
            "expected_error": "Table 'test_db.non_existent_table' doesn't exist"
        }
    ]
}